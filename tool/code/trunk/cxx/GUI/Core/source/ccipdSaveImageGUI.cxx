


//////////////////////////////////////////////////////////////////////////////////////////

// ccipd includes
#include "ccipdSaveImageGUI.hxx"

//////////////////////////////////////////////////////////////////////////////////////////



namespace ccipd
{



//////////////////////////////////////////////////////////////////////////////////////////
// compile for volume types
template
bool SaveImage< VolumeImageType >(
  const VolumeImageType * const image,
        QWidget         * const parent,
  const bool                    verbose );
//////////////////////////////////////////////////////////////////////////////////////////



//////////////////////////////////////////////////////////////////////////////////////////
// compile for mask types
template
bool SaveImage< VolumeMaskImageType >(
  const VolumeMaskImageType * const image,
        QWidget             * const parent,
  const bool                        verbose );
//////////////////////////////////////////////////////////////////////////////////////////



//////////////////////////////////////////////////////////////////////////////////////////
// compile for 3d warp field types
template
bool SaveImage< VolumeWarpFieldType >(
  const VolumeWarpFieldType * const image,
        QWidget             * const parent,
  const bool                        verbose );
//////////////////////////////////////////////////////////////////////////////////////////




//////////////////////////////////////////////////////////////////////////////////////////
// compile for 2D warp field types
template
bool SaveImage< SliceWarpFieldType >(
  const SliceWarpFieldType * const image,
        QWidget            * const parent,
  const bool                       verbose );
//////////////////////////////////////////////////////////////////////////////////////////



} // namespace ccipd