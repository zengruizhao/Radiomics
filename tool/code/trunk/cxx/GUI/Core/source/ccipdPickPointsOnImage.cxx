


////////////////////////////////////////////////////////////////////////////////////////////////////

// ccipd includes
#include "ccipdPickPointsOnImage.hxx"

#include "ccipdDisableWarningsMacro.h"
  // itk includes
  #include <itkImage.h>
  #include <itkRGBPixel.h> 
  #include <itkPointSet.h>
#include "ccipdEnableWarningsMacro.h"

////////////////////////////////////////////////////////////////////////////////////////////////////



namespace ccipd
{



////////////////////////////////////////////////////////////////////////////////////////////////
template
LandmarkSetPointer PickPointsOnImage< VolumeImageType >(
  const VolumeImageType * const image,
  const unsigned int            slice,
  const bool                    verbose );
////////////////////////////////////////////////////////////////////////////////////////////////



////////////////////////////////////////////////////////////////////////////////////////////////
LandmarkSetPointer PickPointsOnImage(
  const RGBSliceType * const image,
  const bool                 verbose )
{

  // no slices in a 2D image
  const unsigned int slice = 0;
  return PickPointsOnImage< RGBSliceType >( image, slice, verbose );

} // PickPointsOnRGBSlice
////////////////////////////////////////////////////////////////////////////////////////////////



} // namespace ccipd
